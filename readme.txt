For the decision_trees.py file you'll need to have pandas installed.
Pandas is installed on the lab computers so that shouldn't be an issue.

Also rather than using "Yes" and "No" as my response values the decision tree returns boolean values True, False.